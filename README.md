# โปรเจกต์ควบคุมอุณหภูมิด้วย PID Controller + State Machine

## 📋 รายละเอียดโปรเจกต์

โปรเจกต์ระบบควบคุมอุณหภูมิแบบอัตโนมัติด้วย ESP32 โดยใช้หลักการ **PID Control** เพื่อควบคุมตัวทำความร้อน (Heater) ให้อุณหภูมิคงที่ตามที่ตั้งค่าไว้ พร้อมระบบ State Machine สำหรับการตั้งค่าและทดสอบการทำงานแบบต่างๆ

---

## 🎯 ความสำคัญและการนำไปใช้งาน

### ความสำคัญ
- **ควบคุมอุณหภูมิอย่างแม่นยำ**: ใช้ PID Algorithm ควบคุมอุณหภูมิให้คงที่และเสถียร
- **ประหยัดพลังงาน**: ปรับกำลังความร้อนแบบ PWM ไม่ปิด-เปิดแบบ On/Off ธรรมดา
- **ปรับแต่งได้ง่าย**: สามารถปรับค่า PID Parameters (Kp, Ki, Kd) ผ่านสวิตช์ได้เลย
- **ปลอดภัย**: มีระบบตัดไฟอัตโนมัติเมื่ออุณหภูมิเกินกำหนด (Safety Cutoff)

### การนำไปใช้งาน
- 🌡️ **ตู้อบ (Oven)**: ควบคุมอุณหภูมิในการอบอาหาร หรือชิ้นงาน
- 🍺 **ถังหมักเบียร์**: ควบคุมอุณหภูมิในการหมัก (Fermentation)
- 🔬 **เครื่องอบแห้ง**: อบชิ้นงานทางการเกษตรหรืออุตสาหกรรม
- ♨️ **เครื่องทำน้ำร้อน**: รักษาอุณหภูมิน้ำให้คงที่
- 🧪 **Lab Incubator**: ควบคุมอุณหภูมิในการเพาะเลี้ยงเชื้อ

---

## 📐 โครงสร้างโปรเจกต์

```
Project_PID_Fuzzy StateMachine_PID_SWConfig/
│
├── platformio.ini          # ไฟล์ตั้งค่า PlatformIO + Library ที่ใช้
│
├── include/                # ไฟล์ Header สำหรับ Class ต่างๆ
│   ├── DevRelay.h         # Class สำหรับจัดการ Relay (รองรับ Active HIGH/LOW)
│   ├── DevSW.h            # Class สำหรับจัดการสวิตช์พร้อม Debouncing
│   └── README
│
├── src/                    # โค้ดหลักของโปรเจกต์
│   └── main.cpp           # โปรแกรมหลัก (PID Control + State Machine)
│
├── lib/                    # โฟลเดอร์สำหรับ Library เพิ่มเติม (ถ้ามี)
│   └── README
│
└── test/                   # โฟลเดอร์สำหรับ Unit Test (ถ้ามี)
    └── README
```

---

## 🧠 หลักการ PID Control

### PID คืออะไร?

**PID** ย่อมาจาก **Proportional-Integral-Derivative** เป็นระบบควบคุมอัตโนมัติที่นิยมใช้กันมากที่สุดในงานอุตสาหกรรม เพราะสามารถควบคุมค่าต่างๆ (เช่น อุณหภูมิ, ความเร็ว, ความดัน) ให้คงที่ได้อย่างแม่นยำ

### ส่วนประกอบของ PID

1. **P (Proportional)** - สัดส่วน
   - คำนวณจาก**ค่าความผิดพลาดในปัจจุบัน** (Error)
   - ยิ่งผิดพลาดมาก ก็ออกแรงมากขึ้น
   - สูตร: `P_out = Kp × Error`

2. **I (Integral)** - ผลรวม
   - คำนวณจาก**ผลรวมความผิดพลาดที่สะสมมา**
   - แก้ปัญหา Steady-State Error (เบี่ยงเบนไปเรื่อยๆ)
   - สูตร: `I_out = Ki × ∫Error dt`

3. **D (Derivative)** - อัตราการเปลี่ยนแปลง
   - คำนวณจาก**ความเร็วในการเปลี่ยนแปลงของ Error**
   - ช่วยลดการแกว่ง (Overshoot) และเพิ่มความเสถียร
   - สูตร: `D_out = Kd × (dError/dt)`

### สมการ PID Algorithm

```
Output = Kp×Error + Ki×∫Error dt + Kd×(dError/dt)

โดยที่:
  Error = Setpoint - Input (อุณหภูมิที่ตั้งไว้ - อุณหภูมิจริง)
  Kp, Ki, Kd = พารามิเตอร์ที่ต้องปรับแต่ง (Tuning)
  Output = ค่าที่ส่งไปขับตัวทำงาน (PWM Duty Cycle)
```

### การทำงานในโปรเจกต์นี้

1. **อ่านอุณหภูมิ** จาก DS18B20 → นำมาเก็บในตัวแปร `Input`
2. **คำนวณ PID** → เปรียบเทียบกับ `Setpoint` แล้วคำนวณหาค่า `Output`
3. **ส่ง PWM** → ค่า Output (0-255) ถูกส่งไปควบคุมกำลังความร้อน
4. **วนลูป** → ทำซ้ำเรื่อยๆ เพื่อให้อุณหภูมิคงที่

---

## ⚙️ การทำงานแบบ State Machine

โปรเจกต์นี้ออกแบบด้วย **State Machine** เพื่อให้สามารถเปลี่ยนโหมดการทำงานได้ง่าย มีทั้งหมด 5 โหมด:

| โหมด | คำอธิบาย |
|------|---------|
| **Processing** | ทำงานควบคุมอุณหภูมิด้วย PID แบบอัตโนมัติ (โหมดปกติ) |
| **Setpoint Config** | ตั้งค่าอุณหภูมิเป้าหมาย (Target Temperature) |
| **PID Config** | ปรับค่า Kp, Ki, Kd |
| **Manual PWM** | ทดสอบสั่งงาน PWM ด้วยตนเอง (ไม่มี PID) |
| **Manual Relay** | ทดสอบการเปิด-ปิด Relay ด้วยตนเอง |
### State Machine Diagram

<div align="center">
  <img width="800" alt="State Machine Diagram" src="https://github.com/user-attachments/assets/3921726a-f1f0-49cd-9ff6-e139625d8b9e" />
</div>
---

## 🔌 วงจรการต่อใช้งาน ESP32

### บอร์ด ESP32 DevKit V2 (THAITECHZONE)

<div align="center">
  <img width="600" alt="ESP32 DevKit V2 Board" src="https://github.com/user-attachments/assets/745a318d-05c9-4f29-b999-897efe7b5a24" />
</div>

### Pin Configuration

| อุปกรณ์ | ชนิด | GPIO Pin | หมายเหตุ |
|---------|------|----------|----------|
| **DS18B20** | Sensor อุณหภูมิ | GPIO 14 | ต่อ R Pull-up 4.7kΩ ไปยัง 3.3V |
| **Heater** | PWM Output | GPIO 13 | ต่อผ่าน MOSFET/SSR |
| **OLED Display** | จอแสดงผล I2C | SDA=21, SCL=22 | ใช้ 0.96" SSD1306 |
| **Switch 1** | Mode/Enter | GPIO 34 | Input Only (ต้องต่อ R Pull-up 10kΩ) |
| **Switch 2** | Down | GPIO 35 | Input Only (ต้องต่อ R Pull-up 10kΩ) |
| **Switch 3** | Up | GPIO 32 | (มี Internal Pull-up ได้) |
| **Relay 1** | Output | GPIO 17 | Optional (Active LOW) |
| **Relay 2** | Output | GPIO 16 | Optional (Active LOW) |
| **Relay 3** | Output | GPIO 4 | Optional (Active LOW) |

### Wiring Diagram - DS18B20

```
DS18B20                     ESP32
┌─────┐                    ┌──────┐
│ VCC ├─────(3.3V)─────────┤ 3.3V │
│     │                    │      │
│ DQ  ├──────┬─────────────┤ G14  │
│     │      │             │      │
│ GND ├───┐  │             │      │
└─────┘   │  └──[4.7kΩ]─ 3.3V    │
          │                │      │
          └────────────────┤ GND  │
                           └──────┘
```

### Wiring Diagram - Heater Control (MOSFET)

```
       PWM Signal                MOSFET                Heater
        (GPIO 13)              (IRF520N)             12V DC / AC
ESP32                                                 
┌──────┐              ┌─────────────┐            ┌──────────┐
│  G13 ├──[220Ω]──────┤ Gate    (D) ├────────────┤  Heater  │
│      │              │             │            │   (+)    │
│  GND ├──────────────┤ Source  (S) ├──┐    ┌────┤  Heater  │
└──────┘              └─────────────┘  │    │    │   (-)    │
                                       GND   │    └──────────┘
                                             │
                                          Power+
                                        (12V DC)
```

### Wiring Diagram - OLED Display

```
OLED SSD1306            ESP32
┌──────────┐          ┌──────┐
│   VCC    ├──────────┤ 3.3V │
│   GND    ├──────────┤ GND  │
│   SCL    ├──────────┤ G22  │
│   SDA    ├──────────┤ G21  │
└──────────┘          └──────┘
```

### Wiring Diagram - Switches (สวิตช์กด)

```
SW1 (Mode/Enter)         SW2 (Down)           SW3 (Up)
                         
    ┌─[10kΩ]─ 3.3V       ┌─[10kΩ]─ 3.3V      ┌─[10kΩ]─ 3.3V
    │                    │                    │
    ├─────────> G34      ├─────────> G35      ├─────────> G32
    │                    │                    │
   [SW1]                [SW2]                [SW3]
    │                    │                    │
   GND                  GND                  GND

หมายเหตุ: GPIO 34, 35 เป็น Input-Only ไม่มี Internal Pull-up
          ต้องต่อ R 10kΩ ภายนอกเอง!
```

### ภาพการต่อทดลองจริง

<div align="center">
  <img src="https://github.com/user-attachments/assets/44e3113a-d55c-4d64-a4a6-4315f8676953" alt="การต่อวงจรทดลอง ESP32 PID Controller" width="800">
  
  *ภาพตัวอย่างการต่อวงจรทดลองจริงของระบบควบคุมอุณหภูมิด้วย ESP32 พร้อม Sensor DS18B20, จอ OLED และสวิตช์ควบคุม*
</div>

---

## 📦 Library ที่ใช้

| Library | เวอร์ชัน | หน้าที่ |
|---------|----------|---------|
| OneWire | ^2.3.8 | สื่อสารกับ DS18B20 |
| DallasTemperature | ^4.0.6 | อ่านค่าอุณหภูมิจาก DS18B20 |
| Adafruit SSD1306 | ^2.5.9 | ขับจอ OLED |
| Adafruit GFX Library | ^1.11.9 | วาดกราฟิกบนจอ |
| PID | ^1.2.1 | คำนวณ PID Algorithm |

---

## 🚀 คำสั่งการใช้งาน

### 1. การติดตั้งและ Upload โปรแกรม

```bash
# ติดตั้ง PlatformIO Core (ถ้ายังไม่มี)
pip install platformio

# เข้าไปในโฟลเดอร์โปรเจกต์
cd Project_PID_Fuzzy\ StateMachine_PID_SWConfig

# Build โปรเจกต์
pio run

# Upload ลง ESP32
pio run --target upload

# เปิด Serial Monitor ดูข้อมูล
pio device monitor
```

### 2. การใช้งานผ่าน VSCode + PlatformIO

1. เปิด VSCode
2. ติดตั้ง Extension "PlatformIO IDE"
3. เปิดโฟลเดอร์โปรเจกต์
4. คลิก **Build** (✓)
5. เสียบ ESP32 เข้าคอมพิวเตอร์
6. คลิก **Upload** (→)
7. คลิก **Serial Monitor** เพื่อดูข้อมูล

---

## 🎮 การใช้งานสวิตช์ (Control)

| สวิตช์ | การทำงาน | โหมด Processing | โหมด Config |
|--------|----------|----------------|-------------|
| **SW1** (Mode) | กดสั้น: ไม่ใช้งาน<br>**กดค้าง 3 วินาที**: เปลี่ยนโหมด | เข้าสู่โหมดตั้งค่า Setpoint | เปลี่ยนไปโหมดถัดไป |
| **SW2** (Down) | กดสั้น: ลดค่า | - | ลด Setpoint / ลด Kp, Ki, Kd / ลด PWM |
| **SW3** (Up) | กดสั้น: เพิ่มค่า | - | เพิ่ม Setpoint / เพิ่ม Kp, Ki, Kd / เพิ่ม PWM |

### ขั้นตอนการตั้งค่า Setpoint

1. **กดค้าง SW1** (Mode) 3 วินาที → เข้าสู่โหมด **Setpoint Config**
2. กด **SW3** (Up) เพื่อเพิ่มอุณหภูมิเป้าหมาย
3. กด **SW2** (Down) เพื่อลดอุณหภูมิเป้าหมาย
4. **กดค้าง SW1** อีกครั้ง 3 วินาที → บันทึกและกลับสู่โหมดปกติ

### ขั้นตอนการปรับค่า PID (Tuning)

1. **กดค้าง SW1** 3 วินาที จากโหมด Setpoint Config
2. เข้าสู่โหมด **PID Config** → ปรับค่า **Kp** ก่อน
3. กด SW2/SW3 เพื่อปรับค่า Kp
4. **กดค้าง SW1** 3 วินาที → ปรับ **Ki**
5. **กดค้าง SW1** 3 วินาที → ปรับ **Kd**
6. **กดค้าง SW1** 3 วินาที → บันทึกและกลับสู่โหมดปกติ

---

## 📊 การตั้งค่าและการ Tuning PID

### ค่าเริ่มต้น (Default Values)

```cpp
Kp = 5.0   // Proportional Gain
Ki = 0.5   // Integral Gain
Kd = 1.0   // Derivative Gain

Setpoint = 25.0°C  // อุณหภูมิเป้าหมายเริ่มต้น
```

### วิธีการปรับ PID แบบมือใหม่ (Ziegler-Nichols Method)

1. **ตั้ง Ki=0, Kd=0** → เหลือแค่ P
2. **เพิ่ม Kp ทีละน้อย** จนอุณหภูมิเริ่มแกว่ง → จด Kp ค่านี้เป็น **Ku**
3. **จับเวลารอบการแกว่ง** (Oscillation Period) → จด **Tu**
4. **คำนวณค่า PID** ตามสูตร:
   - `Kp = 0.6 × Ku`
   - `Ki = 1.2 × Ku / Tu`
   - `Kd = 0.075 × Ku × Tu`

### เคล็ดลับการ Tuning

| ปัญหา | วิธีแก้ |
|-------|---------|
| ขึ้นช้า, ไม่ถึงเป้า | เพิ่ม **Kp** หรือ **Ki** |
| เกินเป้าแล้วแกว่ง (Overshoot) | ลด **Kp** หรือเพิ่ม **Kd** |
| ค่าผิดพลาดคงเหลือ | เพิ่ม **Ki** |
| แกว่งตลอดเวลา | ลด **Kp** และ **Ki** |

---

## 🛡️ ระบบความปลอดภัย

- ✅ **Safety Cutoff**: อุณหภูมิเกิน **100°C** → ปิด Heater อัตโนมัติ
- ✅ **Output Limit**: PWM ถูกจำกัดไว้ที่ 0-255 (0-100%)
- ✅ **บันทึกค่าลง Memory**: Setpoint และค่า PID บันทึกลง Flash Memory (ไม่หายเมื่อดับไฟ)
- ✅ **Error Detection**: ตรวจจับความผิดพลาดของ Sensor และแสดงบนจอ

---

## 📡 Serial Monitor Output

เมื่อเปิด Serial Monitor ที่ **115200 baud** จะเห็นข้อมูลดังนี้:

```
--- ESP32 PID Ready ---
Settings loaded from memory
Setpoint: 50.00
Kp: 5.00, Ki: 0.50, Kd: 1.00

[Processing Mode]
Temp: 49.25°C | SP: 50.00°C | Output: 128 (50%)
Temp: 49.75°C | SP: 50.00°C | Output: 85 (33%)
Temp: 50.00°C | SP: 50.00°C | Output: 64 (25%)
```

---

## 🧪 การทดสอบ (Testing)

### 1. ทดสอบ Manual PWM Mode

- เข้าโหมด **Manual PWM** (กดค้าง SW1 หลายครั้ง)
- ปรับค่า PWM ด้วย SW2/SW3 (0-255)
- ดูว่า Heater ทำงานตาม % Duty Cycle หรือไม่

### 2. ทดสอบ Sensor

- ดู Serial Monitor → ต้องแสดงอุณหภูมิตามปกติ
- ถ้าแสดง "-127.00°C" หรือ "Error" → Sensor ไม่ถูกต่อ หรือเสีย

### 3. ทดสอบ Relay (ถ้ามี)

- เข้าโหมด **Manual Relay**
- ใช้ SW2 เลือก Relay 1, 2, 3
- ใช้ SW3 เปิด Relay / SW2 ปิด Relay

---

## 📞 ติดต่อผู้พัฒนา

**ผู้พัฒนา**: Nattapholj  
**อีเมล**: Nattapholj@gmail.com  
**LINE ID**: thaitechzone  
**เบอร์โทร**: 0939391546  

---

## 📄 License

โปรเจกต์นี้เปิดให้ใช้งานฟรี สามารถนำไปต่อยอดได้ตามต้องการ  
หากนำไปใช้ในเชิงพาณิชย์ กรุณาติดต่อผู้พัฒนา

---

## 🔧 Troubleshooting (แก้ปัญหา)

| ปัญหา | วิธีแก้ |
|-------|---------|
| จอไม่แสดงผล | ตรวจสอบการต่อ SDA/SCL และที่อยู่ I2C (0x3C หรือ 0x3D) |
| อุณหภูมิแสดง -127°C | Sensor ไม่ถูกต่อหรือเสีย / ลืมต่อ R Pull-up 4.7kΩ |
| Heater ไม่ทำงาน | ตรวจ MOSFET/SSR และแหล่งจ่ายไฟ Heater |
| สวิตช์ไม่ทำงาน | GPIO 34, 35 ต้องต่อ R Pull-up ภายนอก 10kΩ |
| ค่าแกว่งไม่หยุด | ลดค่า Kp หรือเพิ่มค่า Kd |

---

## 🎓 เอกสารอ้างอิง

- [PID Controller Theory](https://en.wikipedia.org/wiki/PID_controller)
- [ESP32 Pinout Reference](https://randomnerdtutorials.com/esp32-pinout-reference-gpios/)
- [DS18B20 Datasheet](https://www.analog.com/media/en/technical-documentation/data-sheets/DS18B20.pdf)
- [PlatformIO Documentation](https://docs.platformio.org/)

---

**สุดท้าย**: ขอให้สนุกกับการทำโปรเจกต์! 🚀🔥

---

**Project**: ESP32 PID Temperature Controller  
**Version**: 1.0  
**Last Updated**: February 2026
